[project]
name = "polyglot-sssom-build"
version = "0.0.0"
description = "Development environment for the PolyglotSSSOM Python target."
requires-python = ">=3.12"
dependencies = [
    "fable-library==5.13.0",
]

[tool.uv]
package = false
